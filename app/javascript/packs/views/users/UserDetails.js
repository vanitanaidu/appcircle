import React, { Component } from 'react';

const Users = (props) => {
debugger
  // const renderUserProfile = this.props.users.map(user => (
return (
  <div>
      <div> User Details </div>
       <div> `${props.details.name}`</div>
   </div>
)
}

export default Users
